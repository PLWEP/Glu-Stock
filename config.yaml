debug_mode: true # Set to true to force trades during testing

trading:
    capital: 100000000 # 100M IDR
    risk_per_trade: 0.02 # 2%
    tickers:
        - BBCA.JK
        - TLKM.JK
        - ASII.JK
        - BMRI.JK
        - GOTO.JK

telegram:
    enabled: true # Set to true to enable bot
    # Secrets moved to .env (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
